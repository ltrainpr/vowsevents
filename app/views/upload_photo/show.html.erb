<p id="notice"><%= notice %></p>

  <%= image_tag @upload_photo.s3_image_url.url(:medium) %>

<div class="show actions">
  <%= link_to 'Remove', @upload_photo, confirm: 'Are you sure you want to delete this photo?', method: :delete, class: 'destroy' %>
  <%= link_to 'Back to search photos', images_path %>
</div>