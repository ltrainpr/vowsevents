
<%= form_for @testimonial do |f| %>
	<%= f.label :quote %>
	<%= f.text_field :quote %>
	<%= f.label :event_id %>
	<%= f.select :event_id, options_for_select(Event.all.map{|event| [event.name, event.id]}) %>
	<%= f.submit "Create Testimonial" %>
<% end %>